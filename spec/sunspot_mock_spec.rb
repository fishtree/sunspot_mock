require "spec_helper"

describe SunspotMock do
  it "has a version number" do
    expect(SunspotMock::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
